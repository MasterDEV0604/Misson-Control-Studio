# ADR 002: Session persistence

## Decision
Store a lightweight session snapshot containing selected scenario, active rule pack, workspace layout, and operator preferences.

## Why
The workstation is most believable when it restores the operator's environment between runs.
